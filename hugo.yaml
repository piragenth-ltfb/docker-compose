version: '3'
services:
  hugo:
    container_name: hugo
    image: piragenthdocker/hugo:latest
    volumes:
      - "/home/piragenth/Documents/GitHub/ltfb:/srv/hugo"
    ports:
      - 1313:1313
    command: hugo server -s /srv/hugo --disableFastRender  
    restart: unless-stopped
